[project]
name = "build123d-workspace"
version = "0"
description = "Local build123d projects"
requires-python = ">=3.13,<3.14"
dependencies = [
    "build123d>=0.10,<0.11",
    "filewatcher123d @ git+https://github.com/jdegenstein/filewatcher123d.git",
]
